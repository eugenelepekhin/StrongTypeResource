<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>netstandard2.0</TargetFramework>
		<LangVersion>latest</LangVersion>
		<Nullable>enable</Nullable>
		<PlatformTarget>AnyCPU</PlatformTarget>
		<Version>1.1.8</Version>
		<AnalysisLevel>latest-all</AnalysisLevel>

		<GeneratePackageOnBuild>true</GeneratePackageOnBuild>
		<PackageId>EugeneLepekhin.StrongTypeResource</PackageId>
		<Title>StrongTypeResource</Title>
		<PackageVersion>$(Version)</PackageVersion>
		<Authors>Eugene Lepekhin</Authors>
		<Description>StrongTypeResource is a NuGet package that provides strongly typed access to .NET resources with verification of format specifiers in main and satellite .resx files.</Description>
		<PackageTags>msbuild;resx;.resx;strong;type;resource;method;property;generator;verification;validation;satellite;culture;code;analysis;format;string;formatString;specifier;composite;standard;custom;localization;globalization</PackageTags>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<Title>Strong Type Resource Wrapper Generator</Title>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<PackageProjectUrl>https://github.com/eugenelepekhin/StrongTypeResource</PackageProjectUrl>
		<RepositoryUrl>https://github.com/eugenelepekhin/StrongTypeResource</RepositoryUrl>

		<!-- This is folder where package will unzip binaries. It should be in sync with build/ targets and props. -->
		<BuildOutputTargetFolder>tasks</BuildOutputTargetFolder>
		<NoWarn>$(NoWarn);NU5100;NU5128</NoWarn>

		<PackageReleaseNotes>Adding support for linked files.</PackageReleaseNotes>
		
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Build.Utilities.Core" Version="17.14.28" PrivateAssets="all" ExcludeAssets="Runtime"/>
	</ItemGroup>

	<ItemGroup>
		<Service Include="{508349b6-6b84-4df5-91f0-309beebad82d}" />
	</ItemGroup>

	<ItemGroup>
		<Compile Update="WrapperGenerator.cs">
			<DesignTime>True</DesignTime>
			<AutoGen>True</AutoGen>
			<DependentUpon>WrapperGenerator.tt</DependentUpon>
		</Compile>
	</ItemGroup>

	<ItemGroup>
		<None Update="WrapperGenerator.tt">
			<Generator>TextTemplatingFilePreprocessor</Generator>
			<LastGenOutput>WrapperGenerator.cs</LastGenOutput>
		</None>
	</ItemGroup>

	<ItemGroup>
		<None Include="..\README.md" Pack="true" PackagePath="\" />
		<None Include="build\**" Pack="true" PackagePath="build\" />
	</ItemGroup>

</Project>
